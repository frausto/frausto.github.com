export declare enum ContextMenuTriggerDataAttributes {
  /**
   * Present when the corresponding context menu is open.
   */
  popupOpen = "data-popup-open",
  /**
   * Present when the trigger is pressed.
   */
  pressed = "data-pressed",
}