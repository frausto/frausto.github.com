export declare enum NavigationMenuTriggerDataAttributes {
  /**
   * Present when the corresponding navigation menu is open.
   */
  popupOpen = "data-popup-open",
  /**
   * Present when the trigger is pressed.
   */
  pressed = "data-pressed",
}